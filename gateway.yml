server:
  port: 9999

zuul:
  ribbon:
    eager-load:
      enabled: true
      clients: study-service
  ignoredServices: '*'
  host:
    connect-timeout-millis: 10000
    socket-timeout-millis: 10000


